using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using SWNetwork;
using UnityEngine.UI;

public class TurnSystem : MonoBehaviour
{
 /* public GameObject DrawCards;
  public GameObject Submit;
  public GameObject Ready;
  public GameObject Finalize;
  public Text Turn;

  public bool Debugging = false;
  Game gg;

  void Update(){
    if(Debugging=true){
      
                    if(gg.playerId[1].Equals(NetworkClient.Instance.PlayerId)){
                        Turn.text = "Your Turn to Select the Card";
                    }else{
                        Turn.text = gg.playerName[1]+"Turn to Select the Card";
                    }
        
    }
  }

  public void onfinalButtonClick(){
        Debugging=true;
         if(gg.playerId[1].Equals(NetworkClient.Instance.PlayerId)){
                    DrawCards.SetActive(true);
                    Submit.SetActive(true);
                    Finalize.SetActive(false);
                    Ready.SetActive(true);
                    }else{
                        Debugging = false;
                    }
  }*/
}